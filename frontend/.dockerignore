# Не копировать локальные env в образ — production-сборка должна использовать
# относительные дефолты (/api, same-origin WS), а не dev-значения localhost.
.env
.env.*

# Артефакты, которые не нужны в build-контексте.
node_modules
dist
